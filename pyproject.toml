[project]
name = "radar-fiscalizacao-municipal"
version = "0.1.0"
description = "Radar de fiscalização municipal contínua — coleta, preserva, verifica e prepara apontamentos para revisão humana"
requires-python = ">=3.11"
dependencies = [
    "httpx>=0.27",
    "pydantic>=2.7",
    "pyyaml>=6.0",
    "python-dateutil>=2.9",
    "pdfplumber>=0.11",      # texto com coordenadas e página — essencial p/ âncora
    "pymupdf>=1.24",         # rápido, e lida com PDF malformado de portal municipal
    "selectolax>=0.3",       # parser HTML rápido
    "rapidfuzz>=3.9",        # casamento de nomes de objeto/empresa
    "typer>=0.12",
    "rich>=13.7",
]

[project.optional-dependencies]
ocr = ["ocrmypdf>=16.0", "pytesseract>=0.3.10", "pillow>=10.3"]
painel = ["fastapi>=0.111", "uvicorn>=0.30", "jinja2>=3.1"]
dev = ["pytest>=8.2", "pytest-cov>=5.0", "ruff>=0.5", "mypy>=1.10", "types-PyYAML", "respx>=0.21"]

[project.scripts]
radar = "radar.cli:app"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/radar"]

[tool.ruff]
line-length = 96
target-version = "py311"
src = ["src", "tests"]

[tool.ruff.lint]
select = ["E", "F", "I", "N", "UP", "B", "A", "C4", "SIM", "ARG", "PTH", "RUF"]
ignore = [
    "E501",
    # Exceções têm nome em português, como o resto do domínio. "NormaNaoConferidaError"
    # seria uma mistura de idiomas que não ajuda ninguém a ler o código.
    "N818",
    # Acentos e sinais tipográficos são esperados: a documentação é em português.
    "RUF001", "RUF002", "RUF003",
]

[tool.ruff.lint.per-file-ignores]
# Dublês de teste implementam protocolos cujos argumentos existem para satisfazer
# a assinatura, não para serem usados.
"tests/**" = ["ARG002", "ARG001"]

[tool.mypy]
python_version = "3.11"
strict = true
# Typer monta a CLI com decoradores não tipados; exigir tipagem estrita neles
# produziria ruído sem ganho de segurança.
disallow_untyped_decorators = false
warn_unreachable = true
mypy_path = "src"
packages = ["radar"]

[[tool.mypy.overrides]]
module = ["pdfplumber.*", "fitz.*", "selectolax.*", "ocrmypdf.*", "httpx.*", "typer.*", "rich.*", "yaml.*", "respx.*"]
ignore_missing_imports = true

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]
addopts = "-q --strict-markers"
markers = [
    "rede: exige acesso à internet (não roda na CI padrão)",
    "fixture_real: usa documento público real preservado em tests/fixtures",
]
